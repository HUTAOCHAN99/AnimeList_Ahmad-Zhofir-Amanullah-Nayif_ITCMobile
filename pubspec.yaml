name: myapp
description: "A new Flutter project."

publish_to: 'none' # Jangan publikasikan ke pub.dev

version: 1.0.0+1

environment:
  sdk: ">=3.3.0 <4.0.0" # Menggunakan versi stable Flutter

dependencies:
  flutter:
    sdk: flutter
  hive: ^2.2.3
  hive_flutter: ^1.1.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0
  hive_generator: ^2.0.0
  build_runner: ^2.4.6

flutter:
  uses-material-design: true
